[project]
name = "screensound"
version = "1.3.2"
description = "Detect frozen screen zones and react with sound, auto-Enter, or a Telegram remote control"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pillow>=12.1.1",
    "pynput>=1.8.1",
]
